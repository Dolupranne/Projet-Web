{% extends 'base.html.twig' %}

{% block title %}HistoriqueEnchere{% endblock %}

{% block body %}
    <h1>HistoriqueEnchere</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ historique_enchere.id }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ historique_enchere.prix }}</td>
            </tr>
            <tr>
                <th>Date_enchere</th>
                <td>{{ historique_enchere.dateEnchere ? historique_enchere.dateEnchere|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a class='btn btn-primary' href="{{ path('historique_enchere_index') }}">Retourner a l'historique d'enchère</a></br>

    <a class='btn btn-info' href="{{ path('historique_enchere_edit', {'id': historique_enchere.id}) }}">Modifier l'enhere</a>

    {{ include('historique_enchere/_delete_form.html.twig') }}
{% endblock %}
